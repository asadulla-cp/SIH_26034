# Production environment for Vercel deployment
# Leave VITE_API_URL empty — the frontend will operate in full offline/demo mode.
# All demo products, rules, and dashboard stats will use client-side fallback data.
# To connect a real backend, set this to your deployed API URL (e.g. https://metalex-api.onrender.com)
VITE_API_URL=
