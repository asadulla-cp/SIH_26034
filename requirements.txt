# MetaLex Backend Dependencies for Render deployment
# easyocr and torch (~800MB) are excluded — Gemini handles OCR in production.
# EasyOCR is used as local fallback only when GEMINI_API_KEY is not set.

# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
aiofiles==25.1.0

# Database
SQLAlchemy==2.0.52

# Auth
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0
email-validator==2.3.0

# AI / Vision (lightweight — no torch/easyocr)
google-generativeai==0.8.6
opencv-python-headless==4.10.0.84
pillow==12.3.0
numpy==1.26.4

# PDF generation
reportlab==5.0.1

# Data / utilities
python-dotenv==1.2.3
requests==2.34.2
beautifulsoup4==4.15.0
httpx==0.28.1
openpyxl==3.1.5
python-dateutil==2.9.0.post0
pydantic==2.13.5
